# .github/workflows/schedule.yml
name: Daily DAX Forecast

on:
  schedule:
    - cron: '30 6 * * 1-5' # 06:30 UTC ≈ 08:30 Wien, Mo-Fr
  workflow_dispatch:

jobs:
  forecast:
    runs-on: ubuntu-latest

    steps:
      - name: Repo auschecken
        uses: actions/checkout@v3
        with:
          persist-credentials: false  # Wir benutzen eigenen PAT

      - name: Python Setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Abhängigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy ta

      - name: Forecast ausführen
        run: python dax_trend_forecast.py

      - name: Änderungen committen & pushen
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "Hst332"
          git config --global user.email "hst332@example.com"
          git add result.txt
          git commit -m "📊 Tägliche DAX-Prognose aktualisiert" || echo "Keine Änderungen"
          git push https://$GH_PAT@github.com/${{ github.repository }} HEAD:main
