name: 📊 Daily Gas Forecast

on:
  schedule:
    - cron: '0 7 * * 1-5'  # Läuft Mo–Fr um 07:00 UTC = 08:00 CET
  workflow_dispatch:        # erlaubt manuelles Starten

jobs:
  run-forecast:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests pandas numpy ta matplotlib

      - name: Run forecast script
        env:
          TRADINGECONOMICS_KEY: ${{ secrets.TRADINGECONOMICS_KEY }}
        run: |
          echo "🚀 Starte Gas Forecast..."
          python gas_forecast_daily.py || echo "⚠️ Script failed but continuing..."
          echo "✅ Forecast abgeschlossen oder übersprungen."

      - name: Commit result.txt
        run: |
          echo "📦 Committe result.txt..."
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add result.txt || echo "Keine result.txt gefunden"
          git commit -m "📊 Tägliche Gas-Prognose aktualisiert" || echo "Keine Änderungen"
          git push
