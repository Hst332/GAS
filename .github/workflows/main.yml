# .github/workflows/schedule.yml
name: 📈 Daily DAX Forecast

on:
  schedule:
    - cron: '30 6 * * 1-5' # 06:30 UTC ≈ 08:30 Wien, Monday–Friday
  workflow_dispatch:

jobs:
  forecast:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false  # use our own PAT instead

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy ta

      - name: 🔮 Run DAX forecast script
        run: python dax_trend_forecast.py

      - name: 💾 Commit and push results
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "Hst332"
          git config --global user.email "hst332@example.com"

          git add result.txt
          git commit -m "📊 Tägliche DAX-Prognose aktualisiert" || echo "Keine Änderungen zum Commit"

          # Use PAT for authentication
          git push https://x-access-token:${GH_PAT}@github.com/${{ github.repository }} HEAD:main
