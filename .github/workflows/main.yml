# .github/workflows/schedule.yml
name: 🔥 Daily Natural Gas Forecast

on:
  schedule:
    - cron: '30 6 * * 1-5' # 06:30 UTC ≈ 08:30 Wien, Montag–Freitag
  workflow_dispatch:

jobs:
  forecast:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Repository auschecken
        uses: actions/checkout@v4
        with:
          persist-credentials: false  # wir nutzen unseren eigenen PAT

      - name: 🐍 Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Abhängigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy ta

      - name: 🔮 Erdgas-Prognose ausführen
        run: python gas_trend_forecast.py

      - name: 💾 Änderungen committen & pushen
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "Hst332"
          git config --global user.email "hst332@example.com"

          git add result.txt
          git commit -m "🔥 Tägliche Erdgas-Prognose aktualisiert" || echo "Keine Änderungen zum Commit"

          # Push mit Personal Access Token
          git push https://x-access-token:${GH_PAT}@github.com/${{ github.repository }} HEAD:main
