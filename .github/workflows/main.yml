name: 📊 Daily Gas Forecast

on:
  schedule:
    - cron: '0 7 * * 1-5'   # Läuft Montag–Freitag um 07:00 UTC = 08:00 CET
  workflow_dispatch:         # Erlaubt manuelles Starten über GitHub UI

permissions:
  contents: write            # Erforderlich, damit der Bot result.txt committen darf

jobs:
  run-forecast:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: 📦 Install dependencies
        run: |
          pip install requests pandas numpy ta matplotlib

      - name: 🔮 Run Gas Forecast Script
        env:
          TRADINGECONOMICS_KEY: ${{ secrets.TRADINGECONOMICS_KEY }}
        run: python gas_forecast_daily.py

      - name: 🧾 Commit & Push result.txt
        if: success()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Sicherstellen, dass Änderungen erkannt werden
          git add result.txt || echo "⚠️ Keine result.txt gefunden"
          git diff --cached --quiet && echo "⚠️ Keine Änderungen zu committen" || (
            git commit -m "📊 Tägliche Gas-Prognose aktualisiert"
            git push origin main
          )
