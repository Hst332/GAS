name: Hourly Gas Forecast (Mon–Fri 07–22)

on:
  schedule:
    # Läuft stündlich von 07:00 bis 22:00 Uhr (Montag–Freitag)
    - cron: '0 7-22 * * 1-5'
  workflow_dispatch:

jobs:
  run-forecast:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install requests pandas numpy ta matplotlib

      - name: Run forecast script
        env:
          TRADINGECONOMICS_KEY: ${{ secrets.TRADINGECONOMICS_KEY }}
        run: |
          echo "🔮 Starte Gasprognose..."
          python gas_forecast_daily.py

      - name: Commit result
        run: |
          echo "📦 Committe result.txt..."
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add result.txt previous_result.txt || true
          git commit -m "📊 Stündliche Gas-Prognose aktualisiert" || echo "Keine Änderungen"
          git push
